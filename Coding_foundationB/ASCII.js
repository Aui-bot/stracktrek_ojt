/**
 * Prints the ASCII value of all uppercase alphabets
 */
for (let c = 65; c <= 90; c++) {
    console.log(String.fromCharCode(c) + " = " + c);
}

for (let i = 97; i <= 122; i++) {
    console.log(String.fromCharCode(i) + " = " + i);
    
}